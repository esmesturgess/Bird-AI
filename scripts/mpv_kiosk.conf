# Input bindings for the unattended installation. Used with --no-input-default-bindings,
# which disables EVERYTHING mpv normally binds (seeking, quitting, fullscreen toggle,
# volume, etc.) — this file is the only thing that then does anything at all. Deliberately
# minimal: one key, one action.
SPACE cycle pause
