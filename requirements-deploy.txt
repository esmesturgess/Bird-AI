# Trimmed runtime deps for the live/demo device (Intel NUC, x86 Linux).
# Inference + playback only — NOT the offline clustering/training/reporting stack.
# Install birdnet-analyzer separately (pulls tensorflow, kagglehub, etc.):
#     pip install "birdnet-analyzer[embeddings]"
# Excluded vs requirements.txt: umap-learn, hdbscan, matplotlib, jinja2 (offline-only).
# sounddevice also needs the PortAudio system library (see scripts/setup_nuc.sh).
numpy>=1.26
scipy>=1.12
sounddevice>=0.4
librosa>=0.10
soundfile>=0.12
pandas>=2.2
scikit-learn>=1.4
tqdm>=4.66
ml-collections>=0.1
perch-hoplite>=0.1.1
torch>=2.11

# --- added 2026-09-08 for the installation runtime (live_soundscape.py) ---
# avex provides the second frozen encoder used by the trained heads (1792-d = 768 + 1024).
# It downloads a ~400 MB checkpoint to ~/.cache/esp on first run — do this BEFORE the
# exhibition, not on the night.
avex>=1.3
sounddevice>=0.4        # playback; on Linux also: sudo apt install libportaudio2
